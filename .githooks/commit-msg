#!/bin/sh
# Evita que quede Co-authored-by de Cursor en los commits
grep -v '^Co-authored-by: Cursor' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
grep -v '^Made-with: Cursor' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
exit 0
